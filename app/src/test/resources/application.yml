server:
  port: 8082

swagger:
  title: pong
  description: pong
  include: /api/.*

pong:
  rest: false
  servicebus: false

spring:
  application:
    name: pong
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false